import React from 'react';
import { withStyles } from '@material-ui/core/styles';

const UserEditIndex=(props)=>{
  return (
    <div>
      <p>user edit</p>
    </div>
  );
}

const styles = {
}

export default withStyles(styles)(UserEditIndex);